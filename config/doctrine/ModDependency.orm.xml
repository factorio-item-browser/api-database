<doctrine-mapping
    xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                        https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <entity name="FactorioItemBrowser\Api\Database\Entity\ModDependency">
        <options>
            <option name="comment">The table holding the dependencies between the mods.</option>
        </options>

        <id name="mod" association-key="true" />
        <id name="requiredMod" association-key="true" />

        <many-to-one field="mod" target-entity="Mod" inversed-by="dependencies">
            <join-column name="modId" />
        </many-to-one>

        <many-to-one field="requiredMod" target-entity="Mod" fetch="EAGER">
            <join-column name="requiredModId" />
        </many-to-one>

        <field name="requiredVersion" length="16">
            <options>
                <option name="comment">The required version of the mod.</option>
            </options>
        </field>

        <field name="type" column-definition="ENUM('mandatory','optional')">
            <options>
                <option name="comment">The type of the dependency.</option>
            </options>
        </field>
    </entity>
</doctrine-mapping>
