<doctrine-mapping
    xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                        https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <entity name="FactorioItemBrowser\Api\Database\Entity\Mod">
        <options>
            <option name="comment">The table holding the imported mods.</option>
        </options>

        <id name="id" type="integer">
            <generator />
            <options>
                <option name="unsigned">true</option>
                <option name="comment">The internal id of the mod.</option>
            </options>
        </id>

        <field name="name" length="255" unique="true">
            <options>
                <option name="comment">The name of the mod.</option>
            </options>
        </field>
        
        <field name="author">
            <options>
                <option name="author">The author of the mod.</option>
            </options>
        </field>

        <field name="currentVersion" length="16">
            <options>
                <option name="comment">The current version of the mod that has been imported.</option>
            </options>
        </field>

        <field name="order" type="integer">
            <options>
                <option name="unsigned">true</option>
                <option name="comment">The order position of the mod, 1 being the base mod.</option>
            </options>
        </field>

        <one-to-many field="dependencies" target-entity="ModDependency" mapped-by="mod" />

        <one-to-many field="combinations" target-entity="ModCombination" mapped-by="mod" />
    </entity>
</doctrine-mapping>
